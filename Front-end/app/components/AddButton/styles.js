import { StyleSheet } from 'react-native';
import colors from '../../data/colors'

/** 
 * Styles for the AddButton
 */

export default StyleSheet.create({
    button: {
        bottom: '12%', 
        left: '83%',  
        backgroundColor: colors, 
        width: 50,
        height: 50,
        borderRadius: 100
    },
})